---
phase: 04-live-data-feeds
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - FEED-04

must_haves:
  truths:
    - "Clicking Run Analysis button populates RiskFactor rows from live APIs and shows riskFactorsCreated in success message"
    - "The FLOOD, STORM, EARTHQUAKE, WILDFIRE categories appear in the DB after a successful analysis run"
    - "NGO green pins appear on the map when the NGO Locations checkbox is enabled"
    - "All three map layers are independently toggleable without page reload"
---

<objective>
Human verification that both implementation plans (04-01 backend + 04-02 frontend) work end-to-end in the browser.

Purpose: Confirm live API ingestion produces visible results and the NGO map layer works.
</objective>

<execution_context>
@C:/Users/rajee/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 04-01: Live risk ingestion (Open-Meteo weather + GloFAS flood + NASA EONET + optional OWM) wired into Run Analysis button. Schema extended with FLOOD/STORM/EARTHQUAKE/WILDFIRE categories.

    Plan 04-02: NGO Locations as a 4th toggleable green-pin layer on the hotspot map.
  </what-built>
  <how-to-verify>
    **Start dev server:** `pnpm dev`

    **Test 1 — NGO Layer (no auth needed if you have NGO_ADMIN/PLATFORM_ADMIN):**
    1. Log in as PLATFORM_ADMIN and visit `/hotspots`
    2. Confirm 3 checkboxes appear in the map toolbar: "Historical incidents", "Predictive risk zones", "NGO Locations"
    3. Toggle "NGO Locations" off → green pins disappear. Toggle back on → pins reappear
    4. Click a green pin → popup shows NGO name, city, active case count
    5. Check map legend bottom-right — green circle entry appears when NGO toggle is on

    **Test 2 — Live ingestion (PLATFORM_ADMIN required):**
    1. Click "Run Analysis" button
    2. Wait for response (may take 3–10 seconds — fetching external APIs)
    3. Confirm success message format: "{N} signals ingested · {N} zones scored · {N} surges triggered"
    4. Check DB: `SELECT category, source, COUNT(*) FROM "RiskFactor" WHERE source IS NOT NULL GROUP BY category, source;`
       — should show rows with source='open-meteo', 'eonet', and optionally 'owm'

    **Test 3 — Idempotency (run analysis twice):**
    1. Click "Run Analysis" a second time immediately after the first
    2. The risk scores should be similar (not doubled) because stale live factors are deleted before re-ingestion
  </how-to-verify>
  <resume-signal>Type "approved" if all three tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<output>
After approval, create `.planning/phases/04-live-data-feeds/04-03-SUMMARY.md` and update `.planning/STATE.md` marking Phase 04 complete.
</output>
